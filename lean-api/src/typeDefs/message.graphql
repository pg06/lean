scalar Date

type Message {
  _id: ID!
  content: String!
  room: Room
  user: User
  type: String!
  timestamp: Date!
}

type Query {
  messages: [Message!]
}

type Mutation {
  createMessage(content: String!, userId: String!, roomId: String!): Message
}
